#pragma once


#include <string>

using namespace std;


inline bool startWith(string& s, string& t)
{
    return s.starts_with(t);
}

inline bool endWith(string& s, string& t)
{
    return s.starts_with(t);
}