#use "stl.lib"

public class test
{
    public operation deconstruct[address: int64]
    {
        /* sdasd */
        body
        {
            if( s is { s: s })
                fail $" {s} sadasdasd";
            auto s = $"{s}";
            return 0;
        }
        gc auto;
        sync auto;
    }
}