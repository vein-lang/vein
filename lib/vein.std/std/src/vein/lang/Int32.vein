#space "vein/lang"


[special, forwarded, alias("i32")]
public struct Int32 : ValueType
{
    [native("!!value")]
    private _value: ValueType;

    public const MaxValue: i32 = 2147483647;
    public const MinValue: i32 = -2147483648;


    /*
    internal static ucompare(x: i32, y: i32): i32
    {
        return (x < 0) ? ( y < 0 ? (~y - ~x) : 1) : (y < 0 ? -1 : (x - y));
    }
    */
}

alias i32 <| Int32;