#space "vein/lang"


[special, forwarded, alias("u8")]
public struct Byte : ValueType
{
    [native("!!value")]
    private _value: ValueType;

    public foo(): void 
        |> Call.call();

    public foo(): void {
        while(true) {
            let a = 15;
        }
    }
}

alias u8 <| Byte;