#space "std"

public class Testable
{
    public static master(): Void
    {
        auto s = new Array<i32>(1);
        auto w = s[0];

        auto b = as<i64>(0);
        auto cs = nameof(master);
    }
}